server:
  name: "inventory-service"
  mode: "debug"
  port: "51003"
  metrics_port: "9103"

inventory:
  service_name: "inventory-service"
  address: "127.0.0.1:51003"
  store: "memory" # release 环境改为 redis
  redis_addr: "127.0.0.1:6379"
  redis_password: ""
  redis_db: 2
  purchase_limit: 5
  stock: {1: 100} # 初始库存，仅对不存在的键生效（SetNX 幂等），可用环境变量 SECKILL_INVENTORY_STOCK 覆盖

mq:
  url: "" # 推荐通过环境变量 SECKILL_MQ_URL 注入

etcd:
  addr: "127.0.0.1:2379"
